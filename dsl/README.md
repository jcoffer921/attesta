# DSLs

This directory will hold two Racket domain-specific languages used by Attesta's
deterministic rule engine. They are kept separate from the Django application
code; the `apps/validation` app calls into them but does not implement rule
logic itself.

Not yet implemented.
